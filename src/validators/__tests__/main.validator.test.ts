describe('Main validator', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  describe('validateClass', () => {
    it.skip('should validate specified class', async () => {
      expect(true).toBe(false);
    });

    it.skip('should return bad request on validation error', async () => {
      expect(true).toBe(false);
    });
  });
});
