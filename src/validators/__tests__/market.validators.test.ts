describe('Market validators', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  describe('validateMarket', () => {
    it.skip('should validate', async () => {
      expect(true).toBe(false);
    });

    it.skip('should return bad request on validation error', async () => {
      expect(true).toBe(false);
    });
  });
});
