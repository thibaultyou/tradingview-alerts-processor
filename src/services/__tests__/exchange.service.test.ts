describe('Exchange service', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  describe('getExchange', () => {
    it.skip('should return exchange instance', async () => {
      expect(true).toBe(false);
    });

    it.skip('should throw if account credentials are invalid', async () => {
      expect(true).toBe(false);
    });

    it.skip('should apply exchange generic configuration', async () => {
      expect(true).toBe(false);
    });

    it.skip('should apply exchange specific configuration', async () => {
      expect(true).toBe(false);
    });
  });

  describe('refreshExchange', () => {
    it.skip('should return exchange instance', async () => {
      expect(true).toBe(false);
    });

    it.skip('should throw if account credentials are invalid', async () => {
      expect(true).toBe(false);
    });

    it.skip('should apply exchange specific configuration', async () => {
      expect(true).toBe(false);
    });
  });

  describe('fetchTickerPrice', () => {
    it.skip('should return ticker informations', async () => {
      expect(true).toBe(false);
    });

    it.skip("should throw if ticker can't be fetched", async () => {
      expect(true).toBe(false);
    });
  });

  describe('fetchAvailableMarkets', () => {
    it.skip('should return markets informations', async () => {
      expect(true).toBe(false);
    });

    it.skip("should throw if markets can't be fetched", async () => {
      expect(true).toBe(false);
    });
  });
});
