describe('Trading Executor', () => {
  describe('processTrade', () => {
    it.todo(
      'should call BaseExcahngeService.createCloseOrder if trade direction is CLOSE'
    );

    it.todo(
      'should call BaseExcahngeService.createOpenOrder if trade direction is not CLOSE'
    );
  });
});
