describe('Exchange utils', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  describe('formatExchange', () => {
    it.skip('should return formatted exchange name', async () => {
      expect(true).toBe(false);
    });
  });
});
