describe('Trade routes', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  describe('postTrade', () => {
    it.skip('should succeed', async () => {
      expect(true).toBe(false);
    });

    it.skip('should fail', async () => {
      expect(true).toBe(false);
    });
  });
});
